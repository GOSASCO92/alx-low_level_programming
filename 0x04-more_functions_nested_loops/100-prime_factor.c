#include <stdio.h>
/**
 *main - Entry point
 *Return: 0
 */
int main(void)
{
unsigned long int i, j = 612852475143;

for (i = 3; i < 12057; i += 2)
{
while (j % i == 0 && k != i)
k /= i;
}
printf("%lu\n", k);
return (0);
}
