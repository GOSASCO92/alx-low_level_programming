#include <stdio.h>

/**
 * main - prints reverse lower alphabets
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
		putchar(c);
	putchar('\n');
i	return (0);
}
